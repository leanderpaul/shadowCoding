// Take a string value and print the same

#include<stdio.h>

int main()
{
	char str[20];   	// declaration of char string of size 20
	printf("Enter the string");
	scanf("%s",str);        // gets the string from the user
	printf("%s",str);      // prints the string 
}

