// Take double input and print the same

#include<stdio.h>

int main()
{
	double d;                          // declaration of double variable
	printf("Enter the double value");
	scanf("%lf",&d);                   // gets double value from user
	printf("%lf",d);                   // prints the double value
}
